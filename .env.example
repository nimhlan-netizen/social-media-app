# ── Google Drive ─────────────────────────────────────────────────────────────
# The folder ID from your Drive folder URL:
# https://drive.google.com/drive/folders/THIS_IS_YOUR_FOLDER_ID
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here

# For LOCAL DEV: place service_account.json in project root
GOOGLE_SERVICE_ACCOUNT_JSON=/app/service_account.json

# For COOLIFY / DOCKER: paste the entire contents of service_account.json here (single line, minified JSON)
# How to get it (PowerShell): Get-Content service_account.json -Raw | % { $_.Replace("`n","").Replace("`r","") }
# How to get it (Linux/Mac):  cat service_account.json | tr -d '\n'
GOOGLE_SERVICE_ACCOUNT_JSON_CONTENT=

# ── Gemini ───────────────────────────────────────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# ── Postiz ───────────────────────────────────────────────────────────────────
POSTIZ_API_URL=https://postiz.almostrolledit.com
POSTIZ_API_KEY=your_postiz_api_key_here

# Postiz integration IDs — find in Postiz → Settings → Integrations
POSTIZ_INSTAGRAM_INTEGRATION_ID=
POSTIZ_FACEBOOK_INTEGRATION_ID=
POSTIZ_YOUTUBE_INTEGRATION_ID=

# ── Pipeline ─────────────────────────────────────────────────────────────────
POLL_INTERVAL_SECONDS=60
DATA_DIR=/data
MAX_OUTPUT_SIZE_MB=95
